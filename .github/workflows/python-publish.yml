name: Publish to Replicate

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: replicate/setup@v1
        with:
          token: ${{ secrets.REPLICATE_API_TOKEN }}
      - run: replicate push
